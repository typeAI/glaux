language: scala

scala:
   - 2.11.7

jdk:
   - oraclejdk8


cache:
  directories:
  - $HOME/.ivy2/cache
  - $HOME/.sbt/boot/

script:
  - sbt ++$TRAVIS_SCALA_VERSION -jvm-opts .jvmopts clean test &&
    sbt coverageAggregate
after_success:
  - sbt coveralls
